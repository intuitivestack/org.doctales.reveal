<?xml version="1.0" encoding="UTF-8"?>
<project default="build">
    
    <property name="dita-ot-version" value="3.7"/>

    <target name="get-dita-ot">
        <get src="https://github.com/dita-ot/dita-ot/releases/download/${dita-ot-version}/dita-ot-${dita-ot-version}.zip" dest="."/>
        <unzip src="dita-ot-${dita-ot-version}.zip"/>
        <!--- unzip -q dita-ot-$DITAOT.zip
        - chmod +x dita-ot-$DITAOT/bin/dita
        - chmod +x dita-ot-$DITAOT/bin/ant-->
        
        <!--- dita-ot-$DITAOT/bin/dita -\-install org.doctales.reveal.zip-->
        
        <!--script:
        - dita-ot-$DITAOT/bin/dita -i samples/doctales.ditamap -f reveal -v -Dargs.reveal.css="plugins/org.doctales.reveal/css/doctales.css" -Dargs.reveal.theme="doctales"-->
    </target>
    
    <target name="zip-plugin">
        
    </target>
    
    <target name="build" depends="get-dita-ot, zip-plugin">
        <echo>Hello</echo>
    </target>
</project>
